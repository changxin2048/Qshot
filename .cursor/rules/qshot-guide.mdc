---
description: Qshot 项目全局指南——结构、约束、调试、项目地图（始终生效）
alwaysApply: true
---

# Qshot 项目指南

## 项目概述

**Qshot（子弹搜索）** — Chrome MV3 浏览器扩展，多 AI 批量搜索并排展示 + Ctrl+Q 悬浮层。

技术栈：**纯 Vanilla JS（无框架）**、esbuild 打包、Chrome Extension MV3、零运行时 npm 依赖。

---

## 硬性约束

- **单个 JS 文件 ≤ 500 行**。超出必须拆子模块，参考 `src/iframe/inject/` 的拆分方式。
- CSS 同样控制在 500 行以内；`src/settings/styles/`、`src/popup/styles/`、`src/iframe/styles/` 均为多文件范例。
- 纯数据 JSON（如 `siteHandlers.json`）不受行数限制。
- 禁止引入新的 npm **运行时**依赖（devDependencies 构建工具除外）。
- 不要随便改 `dist/` 下的文件，那是构建产物；源码在 `src/`。

---

## 项目结构

```
src/
├── manifest.json
├── background.js           薄壳入口，import ./background/main.js
├── background/
│   ├── main.js             service worker 主入口编排
│   ├── initial-state.js    首次安装写 storage
│   ├── tabs.js             compare tab 管理
│   ├── warmup.js           预热逻辑
│   ├── sites.js            站点注册表操作
│   ├── shortcut-sync.js    快捷键同步
│   ├── keepalive.js        Service Worker Alarm 保活
│   └── context-menu.js     右键菜单（选中文字后搜索）
│
├── config/
│   ├── baseConfig.js           content_script，注入常量到 window
│   ├── initialState.json / .zh-CN.json / .en.json
│   ├── rules-source.mjs        DNR 规则源，构建生成 dist/config/rules.json
│   ├── siteHandlers.json       AI 站点适配表（1300+ 行纯数据，不拆）
│   └── random-questions/       随机问题语料（zh-CN.txt / en.txt）
│
├── iframe/
│   ├── inject.js / overlay.js / iframe.js   薄壳入口
│   ├── overlay_frame.js    子 frame 轻量转发器（热键/关闭信号转发到 top frame）
│   ├── inject/         注入到所有页面的 content script
│   │   ├── main.js / constants.js / dom-utils.js
│   │   ├── executor.js / editors.js / extractor.js
│   │   ├── sidebar-fix.js / file-paste.js / selection-toolbar.js
│   ├── overlay_main.js     MAIN world 钩子（读页面全局变量）
│   ├── overlay/            Ctrl+Q 悬浮搜索层
│   │   ├── main.js / state.js / styles.js（样式聚合入口，注入 Shadow DOM）
│   │   ├── constants.js / groups-panel.js / history-panel.js / prompts-panel.js
│   │   └── styles-picker.js / styles-dark.js
│   ├── iframe.html / iframe.css（@import 薄壳）
│   ├── styles/         CSS 按功能拆分（01-light / 02-dark-base / 03-dark-panels）
│   └── iframe/             compare 多站点并排页
│       ├── main.js / state.js / layout.js / cards-render.js
│       ├── load-queue.js / send.js / history.js / prompts.js
│       ├── add-site.js / status.js / utils.js / sites-loader.js / file-upload.js
│       ├── export.js（@re-export 薄壳）
│       ├── export-collect.js   IO 层：收集/请求/清洗 + buildExportFilename/downloadFile
│       ├── export-format.js    文本变换：flattenMarkdown / parseQA / renderSingleModelBlock
│       ├── export-sections.js  章节构建：generateExportContent / renderSectionsToFormat 等
│       └── export-modal.js     导出弹窗 UI
│
├── popup/
│   ├── popup.html / popup.css（@import 薄壳） / popup.js（薄壳入口）
│   ├── main.js             popup 主入口
│   ├── styles/         CSS 按功能拆分（01-base-composer ~ 05-dark-mode）
│   ├── state.js / sections.js / prompt-edit-modal.js
│
├── settings/
│   ├── settings.html / settings.css / settings.js（薄壳）
│   ├── styles/             CSS 按功能拆分（01-base ~ 10-dark-mode）
│   └── settings/
│       ├── main.js / state.js / store.js / utils.js / drag.js / import-export.js
│       └── sections/
│           ├── groups.js / custom.js / prompts.js / prompts-editor.js
│           └── random.js / other.js / misc.js / about.js
│
└── shared/
    ├── i18n.js / storage-keys.js / shortcut.js
    ├── prompt-groups.js / prompt-item.js
    └── site-registry.js / diagnostics.js
```

每个入口文件（popup.js / settings.js / iframe.js / inject.js / overlay.js）都是**薄壳**，import `./xxx/main.js` 启动。
**改代码前**：先看入口 → main.js → 周边模块。

---

## 项目地图（改什么去哪找）

| 要改的功能 | 去哪找 |
|-----------|--------|
| AI 站点搜索触发逻辑 | `inject/executor.js` + `config/siteHandlers.json` |
| 添加 / 修改支持的 AI 站点 | `config/siteHandlers.json` + `config/rules-source.mjs` |
| 页面内容提取（截图/复制） | `inject/extractor.js` |
| 富文本编辑器适配（输入框） | `inject/editors.js` |
| Ctrl+Q 悬浮层 UI | `overlay/main.js` + `overlay/styles.js` |
| 悬浮层子 frame 转发 | `iframe/overlay_frame.js` + `overlay_main.js` |
| 悬浮层分组/历史/提示词面板 | `overlay/groups-panel.js` / `history-panel.js` / `prompts-panel.js` |
| compare 页多栏布局 | `iframe/layout.js` |
| compare 页卡片渲染 | `iframe/cards-render.js` |
| compare 页截图导出 | `iframe/export-modal.js` + `iframe/export-collect.js` + `iframe/export-sections.js` |
| compare 页查询分发 | `iframe/send.js` |
| popup 弹窗 UI | `popup/main.js` + `popup/sections.js` + `popup/popup.css` |
| 设置页某个 tab | `settings/settings/sections/<tab>.js` |
| 全局 storage 读写 | `settings/settings/store.js` + `shared/storage-keys.js` |
| 国际化文案 | `shared/i18n.js` |
| 右键菜单（选中搜索） | `background/context-menu.js` |
| 划词搜索气泡 | `inject/selection-toolbar.js` |
| service worker（标签管理等） | `background/main.js` + `background/<对应模块>.js` |
| DNR 请求改写规则 | `config/rules-source.mjs`（不要改 dist/config/rules.json） |

---

## 构建 & 刷新

```bash
npm run watch   # 推荐开发模式，改动自动重建
npm run build   # 单次构建
NODE_ENV=production npm run build  # 发布构建（minify）
```

| 改了哪里 | 刷新方式 |
|---------|---------|
| background / popup / settings / compare 页 | `chrome://extensions/` 点 🔄 刷新图标 |
| content script（inject / overlay） | 🔄 刷新扩展 **+** 关闭并重开目标标签页（不是 F5） |
| 静态资源（html/css/图片） | 🔄 刷新扩展 + 重开相关页面 |

> **content script 不热更新**：MV3 不会自动重注入到已打开的标签页。刷新扩展后必须关闭并重新打开目标标签页。

---

## 新增模块规则

- 新 JS 入口 → 加 `build.mjs` 的 `ENTRIES` + `src/manifest.json`
- 新静态资源 → 加 `build.mjs` 的 `SRC_ASSETS`
- content script 需要 fetch 的扩展内资源 → 加 `manifest.json` 的 `web_accessible_resources`

---

## 已知陷阱

1. **postMessage 跨 origin**：`window.parent.postMessage(data, EXTENSION_ORIGIN)` 必须用 `try/catch`，`all_frames: true` 会把 content script 注入到第三方站点自己的 iframe，`origin` 不是扩展会同步抛错。

2. **content script 旧版本残留**：`inject.js:行号` 在新 dist 里找不到 → 是旧 content script 还在老标签页跑，关标签重开即可。

3. **`web_accessible_resources` 易漏**：fetch 扩展内资源（如 `siteHandlers.json`）需在 manifest 声明，且所有 fetch 要加 `try/catch` 兜底。

---

## 维护本文件（重要）

**每次新增或删除文件/模块后，必须同步更新本文件（`.cursor/rules/qshot-guide.mdc`）：**

- 新增 JS 模块 → 在"项目结构"对应目录下补一行，在"项目地图"补对应条目
- 新增入口或 content script → 在"新增模块规则"和"调试入口"补说明
- 发现新的已知陷阱 → 补到"已知陷阱"里
- 文件被拆分/合并/删除 → 同步修改结构图

不需要等用户提醒，改完代码就顺手更新这里。

---

## Git 约定

- `main` → 跟踪上游 `30bewater/Qshot`，只做 merge/PR
- `dev` → 日常开发，完成一个独立功能就提交一次，方便回退

---

## 调试入口

| 调试目标 | 入口 |
|---------|------|
| service worker | `chrome://extensions/` → Qshot → "检查视图：Service Worker" |
| popup | 右键扩展图标 → 检查弹出内容 |
| compare 页 | 在 compare 页直接 F12 |
| content script | 目标网站 F12 → 顶部 context 切换到扩展 content script world |
